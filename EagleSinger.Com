git clone  cd https://codespaces.new/anne1last-webpage/EagleSinger.Com?quickstart=1

git add redme file for Download install 

Clone (ดาวน์โหลดไฟล์ทั้งหมด)

git commit -m "message" (บันทึกไฟล์)

git push (อัปโหลดไฟล์)
